# Teacher Rights RAG

교육부 **『2025 교육활동 보호 매뉴얼』** 을 근거로 교권(교육활동 보호) 관련 질의에 답하는 Citation 기반 RAG QA 서비스와, 그 품질을 측정하는 Eval Harness, 그리고 Eval 로 검증한 개선 실험 기록.

## 목차

- [실행 방법](#실행-방법)
- [환경 변수](#환경-변수)
- [Corpus](#corpus)
- [아키텍처](#아키텍처)
- [RAG QA 서비스](#rag-qa-서비스)
  - [Ingest 파이프라인](#ingest-파이프라인)
  - [Chunking 전략](#chunking-전략)
  - [Embedding / Index](#embedding--index)
  - [검색·생성 및 Hallucination 방지 (Abstain)](#검색생성-및-hallucination-방지-abstain)
  - [Citation](#citation)
  - [API 스키마](#api-스키마)
- [Design Decision & Trade-off](#design-decision--trade-off)
- [Eval Harness](#eval-harness)
  - [Gold Set](#gold-set)
  - [지표 정의](#지표-정의)
  - [LLM-as-a-Judge 신뢰성](#llm-as-a-judge-신뢰성)
  - [실행과 재현성](#실행과-재현성)
  - [Baseline 결과](#baseline-결과)
  - [지표의 한계와 맹점](#지표의-한계와-맹점)
  - [CI 연동 설계](#ci-연동-설계)
- [개선 실험](#개선-실험)
  - [실험 설계 원칙](#실험-설계-원칙)
  - [실험 1 (검색): Hybrid 검색](#실험-1-검색-hybrid-검색-bm25--dense-rrf)
  - [실험 2 (생성): Evidence-first 프롬프트](#실험-2-생성-evidence-first-프롬프트)
  - [실험 3 (검색): Kiwi 형태소 BM25](#실험-3-검색-kiwi-형태소-분석-bm25)
  - [종합 결론](#종합-결론)
  - [Next Steps](#next-steps)
- [현재 아키텍처의 한계점 및 알려진 이슈](#현재-아키텍처의-한계점-및-알려진-이슈)
- [향후 개선 과제](#향후-개선-과제)

---

## 실행 방법

```bash
# 0. 요구사항: Python 3.12+, uv (https://docs.astral.sh/uv/)
uv sync                          # 1. 의존성 설치 (.venv 자동 생성)
cp .env.example .env             # 2. OPENAI_API_KEY 입력
uv run python -m scripts.ingest --reset   # 3. 문서 Ingest → data/vectorstore/
uv run uvicorn api.main:app --reload      # 4. API 서버 (http://localhost:8000/docs)
uv run pytest                    # 5. 단위 테스트 (외부 API 호출 없음)
uv run python -m eval.run --name baseline   # 6. Eval Harness 실행 → eval/reports/baseline_<시각>/report.md
# 수록된 리포트는 CHAT_MODEL=gpt-4.1, JUDGE_MODEL=gpt-4.1 (기본값) 으로 실행됨. 다른 모델로 바꾸면 수치가 달라진다.
```

같은 작업을 `make` 로도 실행할 수 있다 (`make help` 로 목록 확인):

```bash
make install && make env      # 설치 + .env 생성
make ingest-reset             # 인덱싱
make dev                      # 서버 (자동 리로드)
make test                     # 테스트
make ask Q="교권보호위원회 구성은?"   # 질의 전송
make eval                     # Eval Harness (make eval NAME=exp1)
make eval-smoke               # 유형별 1개씩 7개만
make eval-exp1 NAME=exp1-a    # 개선 실험 1 (RETRIEVAL_MODE=hybrid, chunks.jsonl 필요)
make eval-exp2 NAME=exp2-a    # 개선 실험 2 (PROMPT_MODE=evidence_first)
make eval-exp3 NAME=exp3-a    # 개선 실험 3 (hybrid + BM25_TOKENIZER=kiwi)
make eval-compare A="eval/reports/baseline-*" B="eval/reports/exp1-*"   # Before/After 표
```

질의 예시:

```bash
curl -s -X POST localhost:8000/chat -H 'Content-Type: application/json' \
  -d '{"question": "교육활동 침해 사안 발생 시 학교장의 조치 절차는?"}' | jq
```

## 환경 변수

| 변수                           | 필수 | 기본값                   | 설명                                                                                |
| ------------------------------ | ---- | ------------------------ | ----------------------------------------------------------------------------------- |
| `OPENAI_API_KEY`               | ✅   | –                        | OpenAI(또는 호환 엔드포인트) API 키. 코드에 하드코딩하지 않으며 `.env`는 gitignore. |
| `OPENAI_BASE_URL`              |      | (OpenAI)                 | OpenAI 호환 엔드포인트 (프록시/타 제공자) 사용 시 지정                              |
| `CHAT_MODEL`                   |      | `gpt-4.1`                | 답변 생성 모델. 수록된 baseline·실험 리포트는 모두 이 값으로 실행                   |
| `EMBEDDING_MODEL`              |      | `text-embedding-3-small` | 임베딩 모델 (인덱싱/검색 동일 모델 강제)                                            |
| `CHUNK_SIZE` / `CHUNK_OVERLAP` |      | `1000` / `200`           | 청킹 파라미터 (문자 단위)                                                           |
| `MIN_CHUNK_CHARS`              |      | `50`                     | 이보다 짧은 청크는 인덱스에서 제외                                                  |
| `TOP_K`                        |      | `5`                      | 검색 청크 수                                                                        |
| `RETRIEVAL_MODE`               |      | `dense`                  | `dense` \| `hybrid`(BM25+dense RRF, 개선 실험 1)                                    |
| `BM25_TOKENIZER`               |      | `bigram`                 | `bigram` \| `kiwi`(형태소 분석, 개선 실험 3) — hybrid 일 때만 사용                  |
| `PROMPT_MODE`                  |      | `baseline`               | `baseline` \| `evidence_first`(인용 추출 후 답변, 개선 실험 2)                      |
| `SIMILARITY_THRESHOLD`         |      | `0.3`                    | 최고 관련도가 이 값 미만이면 LLM 호출 없이 '정보 불충분' 반환                       |

전체 목록과 기본값은 `rag/config.py` 참고.

## Corpus

| 문서                      | 발행         | 규모              | 파일                                             |
| ------------------------- | ------------ | ----------------- | ------------------------------------------------ |
| 2025 교육활동 보호 매뉴얼 | 교육부, 2025 | 219p / 298 chunks | `data/raw/2025+교육활동+보호+매뉴얼(배포용).pdf` |

- **유형**: 공개 문서셋 (정부 기관 정책 매뉴얼), 219페이지.
- **선정 이유**
  - 교권 침해 대응 절차, 교권보호위원회 운영, 법령 근거, 서식 등이 **하나의 체계로 정리된 공식 문서**라 질의의 근거를 페이지 단위로 검증할 수 있다.
  - 절차·기준·수치가 많아 **Hallucination이 관측되기 쉬운** 도메인이다 (예: "며칠 이내에 보고해야 하는가").
  - 일반 상식으로는 답하기 어려운 행정 절차가 많아, 모델의 사전 지식이 아닌 **문서 근거 여부**를 평가하기 좋다.
- **재현성**: PDF 원본을 `data/raw/`에 포함. 메타데이터(제목·발행처·연도)는 `data/raw/manifest.json`에 기록하며 Citation과 이 표에 재사용된다.

## 아키텍처

```
                    ┌────────────────── Ingest (scripts/ingest.py) ──────────────────┐
 data/raw/*.pdf ──▶ rag.loaders ──▶ rag.splitter ──▶ rag.embeddings ──▶ rag.vectorstore
   + manifest.json  (pypdf, 정규화,   (Recursive,      (text-embedding-   (Chroma, cosine,
                     doc_id/page)     chunk_id 부여)    3-small)           data/vectorstore/)
                                          │
                                          └──▶ data/processed/chunks.jsonl (Gold Set 근거 참조용)

                    ┌────────────────── Serve (api/main.py) ─────────────────────────┐
 POST /chat ──▶ api.routers.chat ──▶ rag.chain.answer_question
                                        │ 1) rag.retriever.retrieve(q, k)  → (chunk, score)[]
                                        │ 2) top score < threshold ? → status=insufficient (LLM 미호출)
                                        │ 3) 번호 매긴 문맥 + 시스템 프롬프트 → ChatOpenAI
                                        │ 4) 출력 == INSUFFICIENT_CONTEXT ? → status=insufficient
                                        └ 5) 답변 내 [n] 파싱 → citations
                ◀── ChatResponse {answer, status, citations[], model, latency_ms}
```

## RAG QA 서비스

### Ingest 파이프라인

`uv run python -m scripts.ingest [--reset] [--dry-run]`

1. **로드** (`rag/loaders.py`): `data/raw/` 의 PDF/TXT/MD 를 페이지 단위 `Document` 로 읽는다. pypdf 를 직접 사용해 페이지 번호를 1-based 로 기록한다.
2. **정규화**: 공문 PDF 특유의 연속 공백·개행을 정리하고, 텍스트가 없는 페이지(이미지·빈 페이지)는 제외한다.
3. **메타데이터**: `doc_id`(파일명 slug), `title/publisher/year/url`(manifest), `source`, `page`.
4. **청킹** (`rag/splitter.py`) → 5. **임베딩·저장** (`rag/vectorstore.py`, 100개 배치).
5. 통계(문서/페이지/청크 수, 청크 길이 분포) 출력 및 `data/processed/chunks.jsonl` 덤프.

### Chunking 전략

`RecursiveCharacterTextSplitter(chunk_size=1000, chunk_overlap=200, separators=["\n\n", "\n", ". ", " ", ""])`

| 결정                                  | 근거                                                                                                                                   |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 문단(`\n\n`) → 줄 → 문장 순 재귀 분할 | 매뉴얼은 "항목 제목 + 설명 문단 + 번호 목록" 구조. 문단 경계를 최우선으로 존중해야 하나의 절차가 청크 중간에서 끊기지 않는다.          |
| `chunk_size=1000` (문자)              | 한국어 1000자 ≈ 600~800 토큰. 임베딩 한도(8k) 안에서 절차 하나(보통 5~10문장)가 통째로 들어가는 크기. 실제 분포: 평균 688자, 298 청크. |
| `chunk_overlap=200` (20%)             | 항목 경계에서 잘린 문장이 인접 청크에 중복 포함되어 검색 recall 손실을 완화.                                                           |
| `min_chunk_chars=50`                  | 쪽번호·머리글만 남은 조각(9~40자)이 상위 검색 결과를 오염시키는 것을 방지.                                                             |
| `chunk_id = {doc_id}:p{page}:c{idx}`  | Citation 의 안정적 키. 같은 문서·설정으로 재인덱싱해도 동일 ID → Eval 결과 비교 가능.                                                  |

청킹 파라미터는 모두 환경변수로 조정 가능하며 개선 실험의 변수로 사용한다.

### Embedding / Index

- **Embedding**: `text-embedding-3-small`. 한국어 성능이 충분하면서 비용이 large 대비 1/6 수준. 인덱싱·검색 모두 `rag.embeddings.get_embeddings()` 단일 소스를 사용해 모델 불일치를 원천 차단.
- **Vector DB**: Chroma (로컬 영속, `data/vectorstore/`). 외부 인프라 없이 `uv sync` 만으로 재현 가능. `hnsw:space=cosine` 으로 설정해 relevance score 를 0~1 로 해석한다 (abstain 임계값과 Eval 에서 사용).

### 검색·생성 및 Hallucination 방지 (Abstain)

`rag/chain.py` 는 두 단계로 "답변 불가"를 판단한다.

| 단계             | 조건                                                 | 처리                                  | 잡아내는 케이스                                     |
| ---------------- | ---------------------------------------------------- | ------------------------------------- | --------------------------------------------------- |
| 1. 점수 기반     | 최고 cosine relevance < `SIMILARITY_THRESHOLD` (0.3) | **LLM 미호출**, `status=insufficient` | 명백히 무관한 질의 (날씨, 잡담). 결정적이고 비용 0. |
| 2. 프롬프트 기반 | 모델 출력이 `INSUFFICIENT_CONTEXT`                   | `status=insufficient`                 | 주제는 관련 있으나 문서에 답이 없는 질의.           |

시스템 프롬프트는 (1) 참고 자료만 근거로 답할 것, (2) 문장마다 `[n]` 인용, (3) 근거 부족 시 토큰만 출력, (4) 수치·절차는 그대로 인용할 것을 지시한다. `temperature=0`.

임계값 0.3은 초기값이며, Eval Harness 의 Gold Set(답변 불가 질의 포함)으로 abstain 정확도를 측정해 조정한다.

### Citation

- 검색된 청크를 `[1] (제목, p.45)` 형식으로 번호 매겨 프롬프트에 넣고, 답변 본문의 `[n]` 을 정규식으로 파싱해 **실제 인용된 청크만** `citations` 로 반환한다 (등장 순서 유지).
- 모델이 인용 표기를 하나도 하지 않으면 검색 결과 전체를 반환한다 (근거를 숨기지 않기 위함).
- 각 citation 은 `chunk_id / doc_id / title / page / snippet(200자) / score` 를 포함하므로 사용자는 원문 페이지로 바로 검증할 수 있다.

### API 스키마

`POST /chat` — 전체 스키마와 예시는 `http://localhost:8000/docs` 참고.

```jsonc
// Request
{ "question": "교육활동 침해 사안 발생 시 학교장의 조치 절차는?", "top_k": 5 }   // top_k 선택 (1~20)

// Response
{
  "answer": "학교장은 ... 즉시 피해 교원을 보호하고 [1] ... 교권보호위원회에 ... [2]",
  "status": "answered",                // "answered" | "insufficient"
  "citations": [
    { "index": 1, "chunk_id": "2025-교육활동-보호-매뉴얼-배포용:p45:c0",
      "doc_id": "2025-교육활동-보호-매뉴얼-배포용", "title": "2025 교육활동 보호 매뉴얼",
      "page": 45, "snippet": "학교장은 교육활동 침해행위를 인지한 경우 ...", "score": 0.62 }
  ],
  "model": "gpt-4.1",
  "latency_ms": 1830
}
```

오류: 빈 질의 → `422`, LLM/벡터스토어 오류 → `502 {"detail": "upstream error: ..."}`.

## Design Decision & Trade-off

| 결정                                                                      | 대안                                                       | 선택 이유 / 트레이드오프                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------- | ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LangChain 을 얇게 사용 (splitter, embeddings, Chroma 래퍼, ChatOpenAI 만) | LangChain 체인/Agent 전면 사용 · 프레임워크 없이 직접 구현 | 파이프라인 각 단계를 함수로 노출해 내부 동작을 모두 설명·테스트할 수 있게 했다. 로더는 pypdf 직접 호출 (`langchain-community` sunset 회피). 대신 LangChain 의 편의 체인은 쓰지 않아 코드가 조금 더 길다.                                                                                                                               |
| OpenAI SDK + `OPENAI_BASE_URL`                                            | 제공자별 전용 클라이언트                                   | 환경변수 하나로 OpenAI ↔ OpenAI 호환 엔드포인트를 전환. 비용에 따라 저렴한 모델/제공자로 바꾸기 쉽다.                                                                                                                                                                                                                                  |
| 생성 모델 `gpt-4.1` 기본                                                  | `gpt-4.1-mini` (약 1/5 비용)                               | 평가 결과의 재현성을 위해 리포트에 쓴 모델을 기본값으로 둔다. 32문항 × 실행당 약 130회 호출(생성 + Judge) 규모라 gpt-4.1 로도 실행당 수 달러 수준이고, Judge 캐시로 재실행 비용은 더 줄어든다. 비용을 줄여야 하면 `CHAT_MODEL=gpt-4.1-mini` 로 바꿀 수 있으며, 그 경우 Judge(gpt-4.1)와 모델이 달라져 self-preference 우려도 줄어든다. |
| Chroma 로컬                                                               | Pinecone 등 관리형 · FAISS                                 | 외부 계정 없이 재현 가능 + 메타데이터 필터/점수 조회 지원. 대규모 트래픽에는 부적합하나 현재 규모(수백 청크)에는 충분.                                                                                                                                                                                                                 |
| 문자 기반 청킹                                                            | 토큰 기반 · 구조(제목) 기반 청킹                           | 구현이 단순하고 파라미터를 실험 변수로 삼기 쉽다. 매뉴얼의 목차 구조를 활용한 구조 기반 청킹은 개선 실험 후보.                                                                                                                                                                                                                         |
| 2단계 abstain (점수 + 프롬프트)                                           | 프롬프트만                                                 | 점수 단계는 무관한 질의에 LLM 비용을 쓰지 않고 결정적으로 동작한다. 임계값이 너무 높으면 정답 가능한 질의를 거절하는 위험이 있어 Eval 로 조정한다.                                                                                                                                                                                     |
| 동기 응답 (Streaming 미구현)                                              | SSE 스트리밍                                               | 서비스는 MVP 로 두고 Eval Harness 와 개선 실험에 시간을 배분.                                                                                                                                                                                                                                                                          |

## Eval Harness

RAG 응답 품질을 **검색 단계**와 **생성 단계**로 나누어 측정한다. 점수가 낮을 때 "근거를 못 찾은 것"인지 "찾았는데 잘못 답한 것"인지 분리하기 위한 구조다. 지표 정의는 RAGAS 논문(Es et al., 2023)을 따르되 라이브러리를 쓰지 않고 `eval/metrics/` 에 직접 구현했다. 프레임워크를 블랙박스로 쓰지 않기 위해서이고, 프롬프트를 파일로 고정해 버전을 추적하기 위해서다.

```
eval/
├── gold_set.jsonl        # Gold Set 32개
├── human_labels.jsonl    # Judge 검증용 수동 라벨 32개
├── prompts/*.txt         # Judge 프롬프트 5개 (해시로 버전 기록)
├── metrics/              # retrieval(규칙) · abstain(규칙) · ragas(Judge) · correctness(Judge)
├── judge.py              # Judge 클라이언트: temperature 0, seed, JSON 파싱, 디스크 캐시
├── runner.py / report.py # 실행 · 집계 · report.json/md
├── run.py                # 단일 명령 진입점
├── compare.py            # Before/After 비교
└── reports/              # 실행 결과 (baseline 포함)
```

### Gold Set

`eval/gold_set.jsonl`, 32문항. 각 문항은 질의, 답 가능 여부, **근거 페이지**, **수용 기준 체크리스트**(필수/선택)를 가진다.

| 유형             | n   | 예시                                                                           |
| ---------------- | --- | ------------------------------------------------------------------------------ |
| factual          | 8   | 지역교권보호위원회는 몇 명으로 구성되고 임기는?                                |
| procedural       | 6   | 분리조치는 어떤 절차로 진행되고 기간은 어느 정도가 권장되나?                   |
| multi_evidence   | 4   | 행정심판과 행정소송의 청구 기간은 각각 어떻게 다른가? (2개 절 교차)            |
| summary          | 4   | 교원보호공제사업 표준약관의 보장 항목과 한도를 정리해 주세요                   |
| reasoning        | 4   | 과자를 먹다 지도받고 멈췼지만 다시 먹은 학생, 침해행위인가? (사례 → 유형 판단) |
| unanswerable_out | 3   | 형사 고소 시 승소 확률은? (문서 범위 밖)                                       |
| unanswerable_in  | 3   | 회의록은 몇 년간 보존해야 하나? (주제는 범위 안, 세부 정보는 문서에 없음)      |

- **구축 방법**: `data/processed/chunks.jsonl` 로 매뉴얼 원문을 장별로 읽으며 AI 보조로 초안을 작성하고, 근거 페이지를 원문과 대조해 확정했다. 검색 결과 검토 과정에서 같은 근거가 부록 법령·고시에도 실려 있는 경우(예: 침해학생 조치 7종은 p25·36·53·180)를 발견해 근거 페이지에 추가했다. 답 없는 질의 6개는 `chunks.jsonl` 전문 검색으로 부재를 확인했다. 개선 실험 중에도 누락을 1건 더 발견해(q016: 보호자 조치 종류가 p56 점수표에도 있음) 근거 페이지를 추가하고 `eval.rebuild` 로 모든 리포트의 검색 지표를 재계산했다.
- **근거 키를 chunk_id 가 아닌 page 로 둔 이유**: 청킹 파라미터를 바꾸는 실험에서도 gold 를 다시 매핑하지 않고 비교하기 위해서다. 대신 같은 페이지의 무관한 청크도 정답으로 처리되는 관대함이 있다.
- **편향·한계**: 단일 문서(교육부 매뉴얼 1종) 기반이며 작성자 1인이 검수했다. 질의 표현이 문서 용어와 가까워 실제 교사의 구어체 질의보다 검색이 쉬울 수 있다. 절차·위원회 장(Ⅱ·Ⅲ)에 문항이 집중되고 서식·부록은 거의 다루지 않는다. `unanswerable_in` 은 "문서에 없다" 는 판단을 문서 전문 검색에 의존하므로, 표현이 다른 형태로 존재할 가능성을 완전히 배제하지 못한다.

### 지표 정의

| 단계 | 지표                    | 정의                                                                                                                                  | 판정           | 선정 이유                                                                                                                                                |
| ---- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 검색 | **Recall@K**            | gold 근거 페이지 중 상위 K 청크의 페이지에 포함된 비율                                                                                | 규칙           | 결정적·재현 가능. 다중 근거 질의에서 Hit Rate 와 갈라진다.                                                                                               |
| 검색 | **MRR**                 | gold 페이지에 속하는 첫 청크의 1/rank (없으면 0)                                                                                      | 규칙           | 순위에 민감해 리랭킹·하이브리드 실험 효과를 Recall 보다 예민하게 잡는다.                                                                                 |
| 검색 | Context Relevance       | 문맥에서 질문에 필요한 문장만 추출 → 추출 문장 수 / 전체 문장 수 (RAGAS 식 2)                                                         | Judge          | gold 없이도 "불필요한 문맥이 얼마나 섞였나" 를 본다. K·청크 크기 실험의 부작용 감시용.                                                                   |
| 생성 | **Faithfulness**        | 답변을 원자적 주장으로 분해 → 각 주장이 *실제 프롬프트에 들어간 문맥*에서 추론 가능한지 → 지지 주장 수 / 전체 (RAGAS F = \|V\|/\|S\|) | Judge ×2       | Hallucination 직접 측정. 주장 단위라 전체 점수 방식보다 안정적이고 어떤 주장이 지어낸 것인지 로그가 남는다.                                              |
| 생성 | **Correctness**         | 수용 기준 체크리스트 항목별 pass/fail → 통과/전체. 필수 항목 실패 시 0                                                                | Judge          | 요약·추론형 질의에 참조 답변 유사도보다 적합. Faithfulness 와 교차하면 "충실하지만 틀림 = 검색 실패", "맞지만 불충실 = 사전지식 답변" 을 구분할 수 있다. |
| 생성 | **Abstain Accuracy**    | 답 없는 질의의 거부율과 답 있는 질의의 응답률을 **따로** 보고 + 혼동행렬                                                              | 규칙           | "전부 거부" 시스템이 Faithfulness 만점을 받는 것을 견제한다. 두 오류를 분리해야 임계값 실험의 트레이드오프가 보인다.                                     |
| 생성 | Answer Relevance (보조) | 답변에서 질문 3개를 역생성 → 원 질문과 임베딩 cosine 평균 (RAGAS 식 1)                                                                | Judge + 임베딩 | 불완전하거나 장황한 답변 감지. 보조 지표.                                                                                                                |

Abstain 응답은 주장이 0개이므로 Faithfulness·Answer Relevance 계산에서 제외하고 Abstain 지표로만 집계한다. 과잉 거부 케이스는 검색 문맥의 Context Relevance 만 측정해 검색 문제인지 생성 문제인지 분리한다.

### LLM-as-a-Judge 신뢰성

| 확보 방법        | 구현                                                                                                                            |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 결정성           | `temperature=0`, `seed` 고정(기본 42), JSON 구조화 출력. 파싱 실패 시 1회 재시도 후 `judge_error` 로 기록                       |
| 프롬프트 고정    | `eval/prompts/*.txt` 파일로 분리하고 논문처럼 demonstration 1개 포함. 파일 해시를 run_config 에 기록해 프롬프트 변경을 추적     |
| 생성 모델과 분리 | `JUDGE_MODEL`(기본 gpt-4.1)을 생성 모델과 별도로 두어 self-preference 회피                                                      |
| 일관성 측정      | `--repeat 3`: 같은 입력을 3회 채점해 항목별 표준편차와 이진 판정 일치율 보고                                                    |
| Human alignment  | `eval/human_labels.jsonl` 에 32문항 전부 수동 라벨(faithful / correct). Judge 이진 판정과의 일치율과 Cohen's κ 를 리포트에 표시 |

Baseline 측정값 (`eval/reports/baseline-a_20260906-171400`, Judge 3회 반복):

| 지표                      | 3회 반복 표준편차 평균 | 이진 판정 일치율 | 수동 라벨 일치율 (n) | Cohen's κ |
| ------------------------- | ---------------------- | ---------------- | -------------------- | --------- |
| faithfulness (임계값 0.8) | 0.025                  | 87.0%            | 91.3% (23)           | −0.05     |
| correctness (임계값 0.5)  | 0.000                  | 100%             | 100% (32)            | 1.00      |
| context_relevance         | 0.019                  | 96.3%            | –                    | –         |

- **Faithfulness κ 가 음수인 이유**: 23개 중 사람이 "불충실" 로 본 답변이 1개(q015)뿐이라 클래스가 한쪽으로 쏠려 있고, 불일치 2건이 모두 이 희소 클래스 근처에서 났다. Judge 는 q011("21일 이내 개최" 가 문맥에 그대로 있는데 미지지로 판정)에서 사람보다 엄격했고, q015("광역적·정책적 사항" 이라는 문맥에 없는 추론)에서는 사람보다 관대했다. 일치율 91% 와 κ −0.05 가 동시에 나오는 것은 **희소 클래스에서 κ 가 통계적으로 불안정**하다는 뜻이며, 표본을 늘리거나 불충실 답변을 의도적으로 포함한 검증 세트가 필요하다는 한계로 기록한다.
- **Correctness 일치율 100% 의 해석**: 거부 응답 12건은 규칙으로 채점되므로 Judge 판단이 개입하지 않고, 나머지 20건은 체크리스트 기준이 명확해 사람과 Judge 가 같은 결론에 이르렀다. 다만 수동 라벨 작성자가 Judge 결과를 먼저 본 뒤 원문을 대조했으므로 완전한 독립 라벨은 아니다.
- **거부 응답의 Correctness 규칙화 계기**: 3회 반복에서 Judge 가 답 있는 질의의 거부 응답(q013)을 1회는 "기준을 언급하지 않았으니 부정 조건 통과" 로 오판해 1.0 을 준 사례가 있었다. 거부 응답은 판단이 필요 없는 케이스이므로 "답 있는 질의 거부 = 0, 답 없는 질의 거부 = 1" 규칙으로 고정하고, 과거 리포트도 `eval.rebuild` 로 같은 규칙을 적용했다. 이 변경으로 correctness 반복 표준편차가 0.029 → 0.000 이 됐다.

### 실행과 재현성

```bash
make eval                                     # = uv run python -m eval.run --name baseline
uv run python -m eval.run --name x --subset 7 # 유형별 1개 (스모크)
uv run python -m eval.run --name x --repeat 3 # Judge 일관성
uv run python -m eval.run --name x --no-judge # 규칙 지표만 (Judge 비용 0, 생성 호출은 발생)
make eval-compare A=eval/reports/baseline_x B=eval/reports/exp_y   # Before/After 표
```

- 단일 명령으로 Gold Set 전체를 평가하고 `report.md`(요약) · `report.json`(집계) · `items.jsonl`(문항별 상세: 검색 청크·점수·주장별 판정·기준별 판정)을 생성한다.
- **run_config 기록**: git SHA, 실행 시각, 생성/임베딩/Judge 모델, seed, temperature, 청킹·검색 설정, 프롬프트 해시, Gold Set 해시, 인덱스 청크 수, 주요 패키지 버전.
- **Judge 캐시**: `sha256(모델, seed, 프롬프트 해시, 입력, 반복 인덱스)` 키로 `eval/cache/` 에 저장. 같은 답변을 다시 채점하지 않으므로 재실행 비용이 크게 줄고(같은 설정 재실행 시 Judge 호출의 70~80% 가 캐시 적중), 캐시를 지우고 재실행해도 seed 로 동일 결과를 기대한다.
- 생성 단계에도 seed 를 전달하지만 OpenAI 의 seed 는 best-effort 라 재실행 시 답변이 소폭 달라질 수 있다. 실제로 같은 설정의 두 실행(baseline-a/b)에서 faithfulness 가 0.97 / 0.91 로 갈렸다(23문항 중 2문항의 답변이 달라짐). 그래서 실험 비교는 같은 Gold Set·같은 설정에서 **여러 번 실행한 평균**을 권장하며, 리포트에 항목별 점수를 모두 남겨 어느 문항이 흔들렸는지 추적할 수 있게 했다.

### Baseline 결과

설정: gpt-4.1 / text-embedding-3-small / chunk 1000·200 / **top_k 5** / threshold 0.3 / Judge gpt-4.1. 같은 설정으로 2회 실행(`eval/reports/baseline-a_20260906-171400` Judge 3회 반복, `eval/reports/baseline-b_20260906-171542`). 검색·Abstain 지표는 두 실행이 동일했고 생성 지표만 소폭 달랐다.

| 단계    | 지표                | baseline-a    | baseline-b | n   |
| ------- | ------------------- | ------------- | ---------- | --- |
| 검색    | Recall@5            | 0.545         | 0.545      | 26  |
| 검색    | Recall@3 (고정 K)   | 0.449         | 0.449      | 26  |
| 검색    | MRR                 | 0.681         | 0.681      | 26  |
| 검색    | Context Relevance   | 0.115         | 0.115      | 27  |
| 생성    | Faithfulness        | 0.971         | 0.911      | 23  |
| 생성    | Correctness         | 0.651         | 0.651      | 32  |
| 생성    | Answer Relevance    | 0.737         | 0.749      | 24  |
| Abstain | 답 있는 질의 응답률 | 88.5% (23/26) | 88.5%      |     |
| Abstain | 답 없는 질의 거부율 | 83.3% (5/6)   | 83.3%      |     |

유형별 (baseline-a):

| 유형             | n   | Recall@5 | MRR  | Faith | Correct | 거부율 |
| ---------------- | --- | -------- | ---- | ----- | ------- | ------ |
| factual          | 8   | 0.60     | 0.69 | 1.00  | 0.52    | 12.5%  |
| procedural       | 6   | 0.35     | 0.58 | 0.88  | 0.56    | 33.3%  |
| multi_evidence   | 4   | 0.50     | 0.75 | 0.96  | 0.17    | 0.0%   |
| summary          | 4   | 0.81     | 1.00 | 1.00  | 1.00    | 0.0%   |
| reasoning        | 4   | 0.50     | 0.43 | 1.00  | 0.67    | 0.0%   |
| unanswerable_in  | 3   | –        | –    | –     | 1.00    | 66.7%  |
| unanswerable_out | 3   | –        | –    | –     | 1.00    | 100%   |

**관찰**

- **과잉 거부 3건(q004, q009, q013)은 모두 Recall@5 = 0** 이다. 생성 모델이 소극적인 것이 아니라 근거 페이지가 상위 5개에 들지 않아 프롬프트 규칙대로 거부한 것이다. 병목은 검색 단계다. 예: "시·도교권보호위원회 위원 정수" 질의에 p24·26·28(지역교권보호위원회)은 검색되고 p23(시·도)은 빠졌다. 용어가 비슷한 두 위원회를 dense 임베딩이 구분하지 못했다. (top_k 4 → 5 로 늘리면서 q016·q024 두 건은 5위 청크 덕에 거부에서 답변으로 바뀌었다.)
- **답 없는 질의에 답변 1건(q032)**: 본문은 "명시되어 있지 않아 답변드릴 수 없다" 는 사실상의 거부인데 `INSUFFICIENT_CONTEXT` 토큰을 쓰지 않아 `status=answered` 로 집계됐다(soft refusal). 특별휴가 청크가 0.72 로 검색돼 점수 게이트를 통과했고, 프롬프트 규칙의 "정확히 토큰만 출력" 을 모델이 따르지 않은 경우다. Abstain 지표의 맹점이자, 개선 실험 2(인용 추출 후 답변)가 겨냥하는 케이스다.
- **multi_evidence 의 Correctness 0.17**: 두 절을 교차해야 하는 질의에서 한쪽 근거만 검색되면 모델이 나머지를 "자료에 없다" 고 답하거나(q015) 검색된 청크의 인접 조항으로 대체해 답한다(q016). 요약형(summary)은 근거가 한 페이지에 모여 있어 1.00 을 기록했다.
- **Faithfulness 는 높고(0.94) Correctness 는 낮다(0.65)**: 지어내지는 않지만 근거를 덜 가져오거나 일부만 답하는 패턴(q006: 7개 조치 중 4개, q018: 4위 청크의 180일 누락). 개선 실험은 생성보다 검색(Recall) 쪽을 먼저 겨냥해야 한다는 근거가 된다.
- Context Relevance 0.12 는 5개 청크(약 3,400자) 중 실제 필요한 문장이 10% 안팎이라는 뜻이다. 절대값보다 K·청크 크기 실험에서의 변화량으로 해석한다.

### 지표의 한계와 맹점

| 지표              | 맹점                                                                                                                                                                                                                           |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Recall@K / MRR    | 페이지 단위라 같은 페이지의 무관 청크도 정답 처리. gold 근거가 여러 곳에 중복될 때 어느 하나만 찾아도 만점. MRR 은 첫 정답 순위만 반영해 다중 근거 질의에 불리.                                                                |
| Context Relevance | 문장 분할 규칙(종결부호·글머리표 기준)에 의존. 판정자가 추출한 문장 수와 규칙 분할 수의 단위가 완전히 일치하지 않아 근사값이다. 표·서식 페이지에서 낮게 나온다.                                                                |
| Faithfulness      | 문맥을 그대로 복사하면 만점. 재구성 표현을 미지지로 판정하는 보수성(위 alignment 불일치 2건). 주장 분해 단계가 문장을 얼마나 잘게 나누느냐에 따라 분모가 달라진다. 숫자·주체가 미세하게 다른 오류를 supported 로 넘길 수 있다. |
| Correctness       | 수용 기준이 작성자 1인의 판단. 기준에 없는 올바른 정보를 더해도 점수가 오르지 않고, 기준의 표현과 다르게 말하면 fail 될 수 있다. 필수 항목 실패 시 0 으로 만드는 규칙이 부분 정답을 과소평가한다.                              |
| Abstain           | `status` 필드만 보므로 "답변은 했지만 본문에서 '자료에 없다' 고 얼버무린" 경우(q015)를 거부로 잡지 못한다. 거부 자체가 정답인 6문항은 Faithfulness 계산에서 빠져 생성 지표의 n 이 줄어든다.                                    |
| Answer Relevance  | 역생성 질문과 임베딩 모델에 의존. 답변이 틀려도 질문과 주제가 같으면 높게 나온다(사실성 미반영).                                                                                                                               |
| Judge 전반        | 사람 라벨도 작성자 1인이라 alignment 수치 자체에 편향이 있다. 같은 모델(gpt-4.1)이 생성과 판정을 모두 맡으므로 self-preference 가 완전히 제거되지 않는다(JUDGE_MODEL 로 교체 가능).                                            |

### CI 연동 설계

Eval 파이프라인을 회귀 방지 장치로 쓰려면 비용과 비결정성을 통제해야 한다. 제안하는 구성:

| 단계  | 트리거                                                            | 내용                                                                                                                                                                   | 비용 통제                                                                    |
| ----- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| Unit  | 모든 PR                                                           | `pytest` (지표 함수, 캐시 키, 리포트 생성 — LLM 호출 없음) + `ruff`                                                                                                    | 0                                                                            |
| Smoke | 모든 PR                                                           | `eval.run --subset 7 --no-judge` → Recall@K·Abstain 만 검사. 임계값(예: Recall@5 ≥ baseline − 0.1, 답 없는 질의 거부율 = 100%) 미달 시 실패                            | 생성 7회 + 임베딩                                                            |
| Full  | `main` 머지 후 야간 1회, 또는 `eval/`·`rag/` 변경 PR 에 수동 라벨 | `eval.run --repeat 2` 전체 → `compare.py` 로 직전 baseline 과 diff 를 PR 코멘트에 게시                                                                                 | Judge 캐시를 GitHub Actions cache 에 보존해 변경 없는 문항은 재채점하지 않음 |
| Gate  | Full 결과                                                         | Correctness·Faithfulness 가 baseline 대비 −0.05 이상 하락하거나 hallucination(답 없는 질의 답변) 건수가 증가하면 실패. 단일 실행 변동(±0.03)보다 큰 폭만 게이트로 사용 |                                                                              |

- 프롬프트·Gold Set 해시가 바뀐 PR 은 baseline 갱신 PR 로 취급해 게이트를 건너뛰고 새 baseline 을 커밋한다.
- API 키는 저장소 secret 으로만 주입하고 fork PR 에서는 Smoke/Full 을 실행하지 않는다.
- `.github/workflows/eval.yml` 에 Unit 단계와 수동 트리거 Smoke 를 구현해 두었다. Full 단계는 크레딧 사정에 따라 스케줄을 켜는 것을 전제로 설계만 기록한다.

## 개선 실험

Part B baseline 분석에서 드러난 두 병목에 각각 하나씩 가설을 세우고(실험 1 검색, 실험 2 생성), 실험 1 의 결과를 보고 토크나이저를 바꾼 후속 실험(실험 3)을 추가했다. 같은 Eval Harness 로 Before/After 를 비교했으며, 모든 실험은 baseline 2회 vs 실험 2회를 실행해 평균으로 비교했다.

### 실험 설계 원칙

- 한 실험은 한 변수만 바꾼다. 나머지(모델 gpt-4.1, top_k 5, threshold 0.3, 청킹, Gold Set, Judge gpt-4.1, seed 42)는 baseline 과 동일하며 `run_config` 로 확인할 수 있다.
- 생성 답변은 seed 를 줘도 실행 간 ±0.03 변동이 있으므로(baseline-a/b 의 faithfulness 0.97 vs 0.91) **변동 폭보다 작은 차이는 "차이 없음"** 으로 읽는다. `compare.py` 는 여러 실행의 평균과 min~max 범위를 함께 표시한다.
- 가설에는 오를 지표뿐 아니라 **변하지 않아야 할 지표와 떨어질 수 있는 지표**를 미리 적어 두고 결과를 그 기준으로 해석한다.
- 실행 중 Judge 규칙을 하나 바꿨다(거부 응답 Correctness 규칙화, Part B 참고). 모든 리포트를 `eval.rebuild` 로 같은 규칙으로 재집계했으므로 Before/After 는 동일 기준이다.

### 실험 1 (검색): Hybrid 검색 — BM25 + Dense, RRF

**Hypothesis**
매뉴얼에는 "시·도교권보호위원회 / 지역교권보호위원회", "서식6", "제25조제7항", "24시간 이내" 처럼 **표면 형태가 정확히 일치해야 구분되는 용어**가 많다. Dense 임베딩은 의미가 비슷한 두 위원회를 같은 방향에 놓아 엉뚱한 쪽을 상위에 올렸다(q004: p24·26·28 검색, p23 누락). 어휘 일치 기반 BM25 를 결합하면 이런 질의에서 정답 페이지가 상위 5개에 들어올 것이다.

| 예상      | 지표                                                                                      |
| --------- | ----------------------------------------------------------------------------------------- |
| 상승      | Recall@5, MRR. 과잉 거부 3건 중 일부 해소 → 답 있는 질의 응답률 ↑ → Correctness ↑         |
| 유지      | Faithfulness(생성 프롬프트 불변), 답 없는 질의 거부율(게이트는 dense 최고 점수 기준 유지) |
| 하락 가능 | Context Relevance(어휘만 겹치는 청크 유입), unanswerable_in 의 hallucination 위험         |

**구현** (`RETRIEVAL_MODE=hybrid`): `rag/bm25.py` 가 `chunks.jsonl` 로 문자 bigram BM25 인덱스를 만들고, `rag/hybrid.py` 가 dense 상위 20 + BM25 상위 20 을 RRF(k=60)로 결합해 상위 5개를 돌려준다. 반환 점수는 dense cosine 을 유지하고 abstain 게이트도 dense 최고 점수를 그대로 쓴다. 형태소 분석기 대신 bigram 을 쓴 이유는 추가 의존성 없이 조사·띄어쓰기 변이에 강건하기 때문이다.

**Result** (Before: `baseline-a_20260906-171400`, `baseline-b_20260906-171542` / After: `exp1-hybrid-a_20260906-172142`, `exp1-hybrid-b_20260906-172346`, 각 2회 평균)

| 지표                | Before            | After                 | Δ      |
| ------------------- | ----------------- | --------------------- | ------ |
| Recall@5            | 0.545             | **0.692**             | +0.147 |
| Recall@3 (고정 K)   | 0.449             | **0.606**             | +0.157 |
| MRR                 | 0.681             | 0.735                 | +0.054 |
| Context Relevance   | 0.115             | 0.088                 | −0.028 |
| Faithfulness        | 0.941 (0.91~0.97) | 0.940 (0.92~0.96)     | −0.001 |
| Correctness         | 0.651             | **0.771** (0.76~0.79) | +0.120 |
| Answer Relevance    | 0.743             | 0.743                 | 0.000  |
| 답 있는 질의 응답률 | 88.5% (23/26)     | **96.2%** (25/26)     | +7.7pp |
| 답 없는 질의 거부율 | 83.3% (5/6)       | 83.3% (5/6)           | 0.0pp  |

| 유형            | Correctness     | Recall@5    |
| --------------- | --------------- | ----------- |
| factual         | 0.52 → **0.83** | 0.60 → 0.73 |
| procedural      | 0.56 → 0.61     | 0.35 → 0.47 |
| multi_evidence  | 0.17 → **0.42** | 0.50 → 0.65 |
| reasoning       | 0.67 → **0.85** | 0.50 → 0.88 |
| summary         | 1.00 → 0.94     | 0.81 → 0.81 |
| unanswerable_in | 1.00 → 0.83     | –           |

문항별 (두 실행 평균):

| 문항                         | 변화                                                                  | 원인                                                                                                                                      |
| ---------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| q004 시·도위원회 위원 정수   | 거부 → 답변, recall 0 → 1, correct 0 → 1                              | BM25 가 "시·도교권보호위원회" 문자열이 있는 p23 을 4위로 올림                                                                             |
| q009 24시간 보고 절차        | 거부 → 답변, recall 0 → 0.25                                          | p40 검색으로 24시간 규정은 답했으나 5일 발생보고서(p35·43) 미검색 → 필수 기준 실패로 correct 0 유지                                       |
| q015 두 위원회 심의사항 비교 | recall 0.33 → 0.67, correct 0 → 1                                     | 지역위 심의사항 p25 가 검색되어 양쪽 비교 가능                                                                                            |
| q023 퇴근 후 학부모 폭언     | recall 0 → 1, correct 0.5 → 1                                         | "퇴근" 어휘 일치로 p10 FAQ 검색                                                                                                           |
| q006 침해학생 조치 7종       | correct 0 → 1                                                         | p53 조치 기준표가 추가 검색되어 7개 전부 열거                                                                                             |
| q013 조치 미이행 대응        | 거부 유지 (recall 0)                                                  | "미이행" 은 p59·60·139 에 있으나 bigram 이 만든 무의미 토큰("이행", "행하" 등)이 p54·56 과 더 많이 겹쳐 RRF 상위에 못 듦 → 실험 3 의 동기 |
| q007 특별휴가 일수           | recall 1 → 0.5                                                        | p38 이 밀리고 p36 이 들어옴. 답(5일)은 p73 에도 있어 정답 유지                                                                            |
| q032 특별휴가 급여 (답 없음) | 1회는 "급여가 정상 지급된다" 는 **hallucination**, 1회는 soft refusal | 특별휴가 청크가 더 많이 검색되자(p74·73·75) 모델이 세부 정보를 지어냄 — 예상했던 하락                                                     |

**Analysis**

- 예상대로 검색 지표가 올랐고(Recall@5 +0.15, Recall@3 +0.16), 그 효과가 생성 단계로 전달돼 Correctness 가 +0.12 올랐다. 과잉 거부 3건 중 2건이 해소돼 답 있는 질의 응답률이 96% 가 됐다. 개선 폭(+0.12)은 실행 간 변동(±0.03)의 4배로, 노이즈로 보기 어렵다.
- MRR 은 Recall 보다 덜 움직였다(+0.054). RRF 는 두 검색기 모두에서 상위인 문서를 올리므로 **정답이 아예 없던 질의를 구제**하는 데는 강하지만, 이미 검색되던 정답의 순위를 크게 끌어올리지는 못한다. 순위 개선은 리랭커의 몫이다.
- Faithfulness 는 평균이 그대로지만 유형별로는 factual 이 1.00 → 0.87 로 떨어졌다. 원인을 문항 단위로 확인하니 q006·q008 에서 Judge 가 "학급교체·전학·퇴학", "100/150/300만원" 을 미지지로 판정했는데, **이 문자열은 실제 문맥에 그대로 존재**한다. 즉 지표 하락이 아니라 Judge 의 false negative 이며, 두 실행이 같은 답변을 내 Judge 캐시를 공유했기 때문에 동일 오판이 두 번 집계됐다. 긴 목록·표 형태 문맥에서 Judge 검증 단계가 약하다는 한계로 기록한다.
- Context Relevance 는 예상대로 소폭 하락했다(−0.028). 어휘만 겹치는 청크(q007 의 p36 등)가 상위에 섞인 결과다.
- 예상했던 위험이 실제로 나타났다: unanswerable_in q032 에서 hybrid 가 특별휴가 관련 청크를 더 많이 끌어오자 모델이 "급여 정상 지급" 을 지어냈다. 검색을 잘할수록 "주제는 맞지만 세부가 없는 질의" 에서 hallucination 위험이 커진다는 점이 실험 2 의 동기가 된다.
- 종합하면 가설은 **대체로 지지**된다. 검색 병목이 최종 Correctness 를 제한하고 있었고, 어휘 매칭을 더하는 것만으로 +0.12 를 얻었다.

### 실험 2 (생성): Evidence-first 프롬프트

**Hypothesis**
baseline 프롬프트는 문맥을 주고 바로 답하게 한다. 모델은 상위 1~2개 청크에 의존해 답을 구성하고, 하위 순위 청크의 정보(q018 의 4위 청크 "180일")는 놓치거나, 관련 청크가 있으면 세부가 없어도 답을 만들어낸다(q032). 답변 전에 **각 청크에서 질문과 직접 관련된 원문 인용을 먼저 추출**하게 하고 "인용에 없는 내용은 쓰지 말 것, 인용이 없으면 insufficient" 로 규칙을 바꾸면, 5개 청크를 모두 훑게 되어 누락이 줄고 인용 없이 지어내는 답변이 줄 것이다.

| 예상      | 지표                                                                              |
| --------- | --------------------------------------------------------------------------------- |
| 상승      | Faithfulness, 답 없는 질의 거부율(q032 류), multi_evidence·procedural Correctness |
| 유지      | Recall@5, MRR, Context Relevance(검색 불변)                                       |
| 하락 가능 | 답 있는 질의 응답률(인용 못 찾으면 거부), Answer Relevance, latency·토큰          |

**구현** (`PROMPT_MODE=evidence_first`): `rag/evidence_prompt.py` 의 프롬프트는 JSON 하나(`quotes[]`, `answer`, `insufficient`)를 출력하게 하고, `rag/chain.py` 가 이를 파싱해 quotes 가 비면 거부, `[n]` 표시가 없으면 quote 의 source 를 citation 으로 쓴다. JSON 파싱 실패 시 원문 텍스트로 폴백한다(이번 실행에서 폴백 0건). 점수 게이트는 그대로다.

**Result** (Before: 2회 평균 / After: `exp2-evidence-a_20260906-172721`, `exp2-evidence-b_20260906-173916` 2회 평균)

| 지표                      | Before                | After                 | Δ      |
| ------------------------- | --------------------- | --------------------- | ------ |
| Recall@5 / Recall@3 / MRR | 0.545 / 0.449 / 0.681 | 동일                  | 0      |
| Context Relevance         | 0.115                 | 0.116                 | +0.001 |
| Faithfulness              | 0.941 (0.91~0.97)     | **0.982** (0.97~1.00) | +0.041 |
| Correctness               | 0.651                 | 0.677 (0.677~0.677)   | +0.026 |
| Answer Relevance          | 0.743                 | 0.717 (0.71~0.72)     | −0.026 |
| 답 있는 질의 응답률       | 88.5% (23/26)         | 88.5% (23/26)         | 0.0pp  |
| 답 없는 질의 거부율       | 83.3% (5/6)           | 83.3% (5/6)           | 0.0pp  |

| 문항                                 | 변화                                          | 원인                                                                                                                                                          |
| ------------------------------------ | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| q032 특별휴가 급여 (답 없음)         | soft refusal(status=answered) → **정식 거부** | 인용할 문장이 없어 `insufficient` 로 출력. baseline 의 유일한 hallucination 위험 케이스 해소                                                                  |
| q016 학생 조치 vs 보호자 조치        | 답변(틀림) → **거부**                         | 제26조 근거가 미검색 상태에서 baseline 은 인접 조항으로 답을 만들었지만, 인용 강제 후 "근거 없음" 으로 거부. correct 는 0 그대로지만 틀린 답 대신 거부로 바뀜 |
| q029 학교폭력 자체해결제 (범위 밖)   | 거부 → **답변**                               | p56 의 "교육활동 침해 사안에는 자체해결제가 없다" 를 인용하며 "자료에 없다" 고 답변. 내용은 거부에 가깝지만 status 는 answered → 거부율 상쇄                  |
| q013 조치 미이행 대응                | 거부 → **답변(오답)**, 2회 모두               | 검색된 p54 의 "전출교는 전입교에 미이행 사실을 안내" 문장을 인용해 엉뚱한 답 생성. 인용이 하나라도 있으면 답하는 규칙의 부작용                                |
| q025 과자 사례, q014 분쟁조정 불성립 | correct 0.5 → 1, 0.67 → 1                     | 인용을 기준으로 답을 구성하면서 선택 기준(학칙 조치, 30일 종료)까지 포함                                                                                      |
| q015 두 위원회 비교                  | faith 1.0 → 0.75 / 0.88                       | 인용 밖 추론("실무적 역할")이 여전히 섞임                                                                                                                     |
| q018 특별휴가 후 요양                | correct 0 유지                                | 4위 청크의 "연 180일" 을 여전히 인용하지 않음 — 가설의 "하위 청크 누락 감소" 는 이 문항에서 확인되지 않음                                                     |

**Analysis**

- Faithfulness 는 +0.041 로 실행 간 변동(±0.03)을 약간 넘고, 두 실행 모두(0.969, 0.995) baseline 의 두 실행(0.911, 0.971)보다 높아 **방향은 일관**된다. 다만 표본이 답변 23개라 문항 1~2개의 판정으로 움직이는 크기이므로 "소폭 개선" 이상으로 해석하지 않는다. Correctness +0.026 은 변동 범위 안이며 두 실행이 정확히 같은 값(0.677)을 냈다. 문항별로는 q025·q014 가 오르고(+0.5, +0.33) 나머지는 그대로다.
- 가설의 핵심이었던 hallucination 억제는 **문항 단위로 2회 모두 재현**됐다. q032 가 정식 거부로 바뀌었고, 틀린 답을 하던 q016 도 거부로 바뀌었다. 그러나 집계 지표(거부율)는 그대로인데, q029 가 반대로 거부 → 답변으로 바뀌어 상쇄됐기 때문이다. q029 와 q013 은 같은 실패 패턴이다. **관련은 있지만 질문에 답하지 않는 문장을 인용**하면 규칙상 답변이 생성된다. "인용이 질문에 직접 답하는가" 를 한 번 더 판단하게 하거나 quotes 의 관련도를 점수화하는 후속 설계가 필요하다.
- 가설의 다른 축이었던 "하위 순위 청크 누락 감소" 는 확인되지 않았다. q018 은 2회 모두 4위 청크의 "연 180일" 을 인용하지 않았다. 인용 추출 지시만으로는 모델이 모든 청크를 고르게 읽게 만들지 못한다.
- Answer Relevance 가 −0.026 으로 예상대로 소폭 떨어졌다. 인용에 묶여 답이 문서 표현을 따라가면서 질문과의 표현 거리가 멀어진 것으로 해석한다.
- 응답률·거부율이 그대로인 것은 과잉 거부가 늘지 않았다는 뜻이라 예상보다 양호하다. 다만 답 없는 질의에서 "자료에 없다" 고 서술하는 soft refusal 이 `answered` 로 집계되는 Abstain 지표의 맹점은 실험 2 에서도 그대로 드러났다.
- 종합하면 가설은 **부분적으로 지지**된다. 인용 강제는 근거 없는 답을 막는 데 효과가 있지만(Faithfulness ↑, hallucination 케이스 해소), 인용 선택 자체가 틀리면 그대로 오답이 되므로 검색 품질에 의존하고, 누락 문제는 해결하지 못한다.

### 실험 3 (검색): Kiwi 형태소 분석 BM25

**Hypothesis**
실험 1 의 문자 bigram 은 조사·띄어쓰기에 강건하지만 토큰 대부분이 의미 없는 두 글자 조합이라 **정밀도가 낮다**. q013 처럼 "미이행" 한 단어가 핵심인 질의에서, 질의의 나머지 bigram 이 무관한 페이지와 더 많이 겹쳐 정답 페이지가 밀렸다. 형태소 분석기(Kiwi)로 조사·어미·기호를 제거하고 명사·수사·용언 어간 같은 내용어만 토큰으로 쓰면, 핵심 용어의 가중치가 살아나 Recall 과 특히 **순위(MRR)** 가 오를 것이다. Kiwi 는 "시·도"를 하나의 명사로, "교권·보호·위원회"를 분리해 인식하므로 두 위원회 구분도 bigram 보다 정확해야 한다.

| 예상      | 지표                                                                                                       |
| --------- | ---------------------------------------------------------------------------------------------------------- |
| 상승      | MRR(어휘 매칭 정밀도 ↑), Recall@5 소폭 ↑, 실험 1 대비 Context Relevance 회복                               |
| 유지      | Faithfulness, Correctness(이미 실험 1 에서 오른 만큼), 답 없는 질의 거부율                                 |
| 하락 가능 | 형태소 분석 오류·사전에 없는 법률 용어로 인한 누락, unanswerable_in 의 hallucination 위험은 실험 1 과 동일 |

**구현** (`RETRIEVAL_MODE=hybrid BM25_TOKENIZER=kiwi`): `rag/bm25.py` 의 `tokenize_kiwi` 가 `kiwipiepy` 로 형태소 분석 후 품사가 N\*(체언)·VV/VA(용언 어간)·XR·XPN·SN(숫자)·SL/SH 인 형태만 남긴다. 나머지(RRF 결합, dense 점수 유지, 게이트)는 실험 1 과 동일하며 토크나이저만 다르다.

**Result** (Before: baseline 2회 / After: `exp3-kiwi-a_20260906-175152`, `exp3-kiwi-b_20260906-175352` 2회 평균. 괄호는 실험 1(bigram) 대비)

| 지표                | baseline | 실험 1 bigram | **실험 3 Kiwi**       | Δ vs baseline | Δ vs 실험 1 |
| ------------------- | -------- | ------------- | --------------------- | ------------- | ----------- |
| Recall@5            | 0.545    | 0.692         | **0.734**             | +0.189        | +0.042      |
| Recall@3 (고정 K)   | 0.449    | 0.606         | **0.638**             | +0.189        | +0.032      |
| MRR                 | 0.681    | 0.735         | **0.767**             | +0.087        | +0.032      |
| Context Relevance   | 0.115    | 0.088         | 0.098                 | −0.017        | +0.010      |
| Faithfulness        | 0.941    | 0.940         | 0.939 (0.938~0.940)   | −0.002        | −0.001      |
| Correctness         | 0.651    | 0.771         | 0.771 (0.771~0.771)   | +0.120        | 0.000       |
| Answer Relevance    | 0.743    | 0.743         | 0.754                 | +0.011        | +0.011      |
| 답 있는 질의 응답률 | 88.5%    | 96.2%         | **98.1%** (96.2~100%) | +9.6pp        | +1.9pp      |
| 답 없는 질의 거부율 | 83.3%    | 83.3%         | 83.3%                 | 0             | 0           |

| 유형            | Correctness (baseline → 실험 1 → 실험 3) | Recall@5 (baseline → 실험 1 → 실험 3) |
| --------------- | ---------------------------------------- | ------------------------------------- |
| factual         | 0.52 → 0.83 → 0.83                       | 0.60 → 0.73 → **0.83**                |
| procedural      | 0.56 → 0.61 → 0.61                       | 0.35 → 0.47 → 0.51                    |
| multi_evidence  | 0.17 → 0.42 → **0.67**                   | 0.50 → 0.65 → 0.65                    |
| reasoning       | 0.67 → 0.85 → 0.79                       | 0.50 → 0.88 → 0.88                    |
| summary         | 1.00 → 0.94 → 0.88                       | 0.81 → 0.81 → 0.81                    |
| unanswerable_in | 1.00 → 0.83 → 0.67                       | –                                     |

| 문항                          | 실험 1 → 실험 3                                | 원인                                                                                  |
| ----------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| q001 침해행위 주체            | recall 0.5 → 1.0                               | "주체" 를 뺀 내용어("교육활동", "침해", "행위")가 p7·p8 양쪽과 정확히 매칭            |
| q007 특별휴가 일수            | recall 0.5 → 1.0                               | 실험 1 에서 밀렸던 p38 복귀                                                           |
| q016 학생 조치 vs 보호자 조치 | correct 0 → 1                                  | p56 보호자 조치 점수표(서면사과·특별교육)가 검색돼 양쪽 비교 가능                     |
| q013 조치 미이행 대응         | 거부 → 1회 답변(오답)/1회 거부                 | 여전히 p59 미검색. 답변한 1회는 p54 의 인접 조항(전학 시 미이행 안내)으로 구성한 오답 |
| q003 지역위 구성              | recall 1.0 → 0.5                               | p20(시행령 원문)이 빠지고 p26(표)만 남음. 답은 p26 에도 있어 정답 유지                |
| q020 보호조치 유형            | correct 1.0 → 0.5                              | 검색 페이지는 같은데 답변 표현이 달라져 선택 기준 1개 실패 — 생성 변동                |
| q032 특별휴가 급여 (답 없음)  | 1회 hallucination → **2회 모두 hallucination** | 특별휴가 청크가 더 정확히 검색될수록 "급여 정상 지급" 을 지어냄                       |

**Analysis**

- 가설대로 **순위 지표가 올랐다**. MRR 이 실험 1 대비 +0.032, baseline 대비 +0.087 로, 실험 1 에서 "정답을 구제하지만 순위는 못 올린다" 고 봤던 한계가 토크나이저 정밀도 문제였음이 부분적으로 확인됐다. Recall@5 도 +0.042 더 올라 답 있는 질의 응답률이 98% 에 이르렀다(과잉 거부는 q013 1회만 남음).
- 그러나 **Correctness 는 실험 1 과 정확히 같다(0.771)**. 유형별로 보면 multi_evidence 가 +0.25 오른 만큼 reasoning·summary 가 내려가 상쇄됐는데, 내려간 쪽(q020, q025)은 검색 페이지가 동일한 문항이라 검색이 아닌 **생성 변동**이다. 즉 실험 3 의 검색 개선이 최종 정답률로 이어지는 폭은 실험 1 에서 이미 대부분 실현됐고, 남은 오답(q009 의 5일 보고서, q018 의 180일)은 검색이 아니라 생성이 근거를 놓치는 문제다.
- Context Relevance 는 예상대로 실험 1 보다 회복됐다(+0.010). 무의미한 bigram 토큰이 끌어오던 어휘 중복 청크가 줄었다.
- q013 은 Kiwi 로도 p59 를 못 찾았다. "미이행" 은 정확히 토큰화되지만 p54(조치 이행 안내)·p56(보호자 조치)이 "조치", "이행", "학교", "교육장" 등 더 많은 내용어를 공유한다. 단어 하나의 희소성보다 문서 길이와 공유 어휘 수가 BM25 점수를 지배하는 경우로, 질의 확장이나 리랭커가 필요한 케이스다.
- 예상했던 부작용이 더 뚜렷해졌다. q032 는 2회 모두 hallucination 을 냈다(실험 1 은 1회). 검색이 정확해질수록 "주제는 맞지만 세부가 없는 질의" 에서 모델이 문맥의 인접 정보로 답을 지어내는 경향이 강해진다. 실험 2 의 인용 강제가 이 위험을 막는다는 점에서, 검색 개선과 생성 제약은 함께 가야 한다.
- 종합하면 가설은 **지지**된다(MRR·Recall 상승, Context Relevance 회복). 다만 최종 Correctness 이득은 실험 1 과 같아, 이 corpus 에서는 bigram → Kiwi 의 효과가 "정답을 더 자주, 더 높이 찾는다" 까지이고 "더 맞게 답한다" 로 이어지지는 않았다. 비용 측면에서 Kiwi 는 모델 로딩(약 1초)과 의존성이 추가되지만 질의당 지연은 무시할 수준이다.

### 종합 결론

|                | 실험 1 (검색, bigram hybrid)                | 실험 2 (생성, evidence-first)                           | 실험 3 (검색, Kiwi hybrid)                          |
| -------------- | ------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------- |
| Recall@5 Δ     | +0.147                                      | 0                                                       | **+0.189**                                          |
| MRR Δ          | +0.054                                      | 0                                                       | **+0.087**                                          |
| Correctness Δ  | **+0.120** (변동의 4배)                     | +0.026 (변동 수준)                                      | **+0.120** (실험 1 과 동일)                         |
| Faithfulness Δ | −0.001                                      | +0.041 (2회 모두 baseline 상회)                         | −0.002                                              |
| 해소한 문제    | 과잉 거부 2건, multi_evidence 근거 부족     | hallucination 1건, 틀린 답 1건을 거부로 전환 (2회 재현) | 과잉 거부 추가 1건 해소, multi_evidence 정답률 0.67 |
| 새로 생긴 문제 | unanswerable_in 에서 hallucination 1회      | 관련 문장 인용에 따른 오답 2건                          | unanswerable_in 에서 hallucination 2회              |
| 비용           | 추가 LLM 호출 없음, BM25 인덱스 메모리 소량 | 프롬프트·토큰 ↑ (답변 길이는 비슷)                      | Kiwi 의존성 + 모델 로딩 약 1초                      |

이 corpus 에서는 **검색 병목 해소가 최종 Correctness 에 훨씬 크게 기여**했다. baseline 관찰("지어내지는 않지만 근거를 덜 가져온다")과 일치하는 결과다. 다만 실험 3 이 보여주듯 검색 지표가 더 올라도 Correctness 는 0.77 에서 멈췄다. 남은 오답은 근거가 검색됐는데도 답변에 반영되지 않는 생성 단계 문제이고, 검색이 좋아질수록 답 없는 질의에서 지어내는 위험이 커진다. 두 실험은 서로를 보완하는 관계로 보인다. 실험 1 이 늘린 검색 범위는 unanswerable_in 의 hallucination 위험을 키우고, 실험 2 는 정확히 그 위험을 줄인다. 다만 이번에는 각 변수를 독립적으로 검증하는 데 집중했고, 두 변경을 함께 적용한 조합 실행은 수행하지 않았다.

### Next Steps

1. **실험 반복 횟수 확대**: 생성 지표는 문항 1~2개로 ±0.03 움직이므로, 실험당 3회 이상 실행해 평균·표준편차로 보고하면 실험 2 처럼 변동 수준의 차이를 판정할 수 있다.
2. **k 스윕 (3, 5, 10)**: Recall@3 고정 지표를 이미 넣어 두었으므로 K 변화의 비용 대비 효과를 비교.

## 현재 아키텍처의 한계점 및 알려진 이슈

1. RAG의 검색 단계에서 검색되지 않은 문서가 발생하는 경우 (Recall의 성능), 생성 단계에서는 이를 복원(복구)해서 제시할 수 없습니다. 그래서 최대한 검색 단계에서 Recall의 성능이 매우 중요하다고 생각합니다.
2. 다중 정보 질문에 대한 답변의 성능을 높이기 위한, 검색 방법이 필요하다고 생각합니다. 단일 검색 & 생성으로는 답변 생성이 어려워서, agentic (ReAct loop) RAG 와 graphDB (지식 온톨로지)가 필요하다고 생각합니다. agentic RAG의 경우에는 trajectory에 대한 기록 및 런타임 디버깅의 어려움이 있을거라고 생각합니다.
3. 여러 문서 중에 지식 정보간의 일치가 아닌 서로 상반된 정보가 있는 경우의 처리가 필요하다고 보이며, RAG의 특성상 vectorDB가 만들어지는 시점에 따라 실제 정보와 업데이트 정보간의 불일치 시간이 발생할 수 있다고 생각합니다.

## 향후 개선 과제

1. 검색 단계의 Recall 성능 향상을 위한 graphDB의 구축 및 성능 metric 자동화를 진행하고자 합니다. graphDB는 neo4j 또는 memGraph 등을 검토하고 있으며, 이를 이용시 성능(답변 성능 및 속도)에 대해서 측정 및 개선하고자 합니다.
2. 추후에는, agentic RAG를 구축하여, tool기반으로 호출, 재호출이 agentic loop로 동작하도록 구축하고, trajectory를 tracing해서, 중간과정에서 일어나는 과정에 대한 분석을 진행해보고자 합니다.
